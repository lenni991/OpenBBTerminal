from datetime import datetime

schema = {
    "price": float,
    "beta": float,
    "market_cap": float,
    "exchange": str,
    "industry": str,
    "website": str,
    "description": str,
    "isin": str,
    "eps": float,
    "dividend_yield": float,
    "free_cash_flow_yield": float,
    "debt_equity": float,
    "return_on_equity": float,
    "date": datetime,
}
